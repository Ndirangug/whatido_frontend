import ProfileSettings from '../../../components/profile/ProfileSettings';
import { SettingsPageContainer } from '../../../styles/profile.styles';

function SettingsPage() {
  return (
    <SettingsPageContainer>
      <ProfileSettings />
    </SettingsPageContainer>
  );
}

export default SettingsPage;
